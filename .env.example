# LM Studio Configuration
LM_STUDIO_URL=http://localhost:1234
LM_STUDIO_MODEL=auto
LM_STUDIO_TIMEOUT=30

# SQL Generation Settings
LM_STUDIO_SYSTEM_PROMPT="You are a DuckDB SQL expert. Convert natural language queries to valid DuckDB SQL. Only respond with the SQL query, no explanations or formatting."
LM_STUDIO_TEMPERATURE=0.3
LM_STUDIO_MAX_TOKENS=500

# Optional: Ollama Configuration (alternative to LM Studio)
# OLLAMA_URL=http://localhost:11434
# OLLAMA_MODEL=codellama:7b